<div class="d-flex flex-column justify-content-between align-items-between" style="height: 80vh">
  <div class="row">
    <div class="col-8">
      <h2><%= @group.name %></h2>
    </div>
    <div class="col-4">
      <p>$<%= @total %></p>
    </div>
  </div>

  <div>
    <% @items.each do |item| %>
      <div class="card mb-2">
        <div class="card-body">
          <div class="row">
            <div class="col-8">
              <%= item.name %>
            </div>
            <div class="col-4">
              $<%= item.amount %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="d-flex flex-column justify-content-between align-items-between">
    <%= link_to "Add new item", new_group_item_path(@group), class: "btn btn-success" %> <br>
    <%= link_to "Back", groups_path, class: "btn btn-secondary" %>
  </div>
</div>